# Not published

The `_` prefix means the site skips this folder. Nothing here is built.

`folding-tables-and-chairs.jpg` — a bare folding table and stacking chairs on a
carpeted floor, with a "2/3" overlay burned into the corner from whatever it
was exported out of. It documents rental stock rather than finished work, and
it is the only photo in the batch that would pull the portfolio's average down
rather than up.

If table and chair hire is a service worth listing, this needs a clean reshoot
of a table actually dressed. Otherwise delete the folder.
